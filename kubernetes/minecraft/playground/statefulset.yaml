# Playground Minecraft Server StatefulSet
# Paper Server with plugin sync from Garage S3

apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mc-playground
  namespace: minecraft
  labels:
    app.kubernetes.io/name: minecraft
    app.kubernetes.io/component: playground
spec:
  serviceName: mc-playground
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: minecraft
      app.kubernetes.io/component: playground
  template:
    metadata:
      labels:
        app.kubernetes.io/name: minecraft
        app.kubernetes.io/component: playground
    spec:
      # プラグイン/スクリプト同期用 init-container
      initContainers:
        - name: sync-plugins
          image: rclone/rclone:latest
          command:
            - sh
            - -c
            - |
              echo "=== Syncing plugins from Garage S3 ==="

              # 共通プラグインを同期
              echo "Syncing shared plugins..."
              rclone sync garage:krz-plugins/shared/ /data/plugins/ \
                --config /config/rclone.conf \
                --verbose

              # Playground専用プラグインを同期
              echo "Syncing playground plugins..."
              rclone sync garage:krz-plugins/playground/ /data/plugins/ \
                --config /config/rclone.conf \
                --verbose

              # Skriptスクリプトを同期 (Playground用のみ)
              echo "Syncing Skript scripts..."
              mkdir -p /data/plugins/Skript/scripts

              # 共通スクリプト
              for dir in krz-core krz-economy krz-stats krz-leveling krz-playground; do
                rclone sync "garage:krz-scripts/$dir/" "/data/plugins/Skript/scripts/$dir/" \
                  --config /config/rclone.conf \
                  --verbose
              done

              echo "=== Sync completed ==="
              ls -la /data/plugins/
          volumeMounts:
            - name: rclone-config
              mountPath: /config
              readOnly: true
            - name: data
              mountPath: /data
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi

      containers:
        - name: minecraft
          image: itzg/minecraft-server:java21
          env:
            - name: EULA
              value: "TRUE"
            - name: TYPE
              value: "PAPER"
            - name: VERSION
              value: "1.21.3"
            - name: MEMORY
              value: "4G"
            - name: ENABLE_RCON
              value: "true"
            - name: RCON_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mc-playground-secrets
                  key: rcon-password
          ports:
            - containerPort: 25565
              name: minecraft
            - containerPort: 25575
              name: rcon
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              cpu: 1000m
              memory: 4Gi
            limits:
              cpu: 4000m
              memory: 8Gi
          livenessProbe:
            exec:
              command:
                - mc-health
            initialDelaySeconds: 120
            periodSeconds: 60
          readinessProbe:
            exec:
              command:
                - mc-health
            initialDelaySeconds: 60
            periodSeconds: 10

      volumes:
        - name: rclone-config
          secret:
            secretName: rclone-config

  # 永続ボリューム (ワールドデータ)
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 30Gi
        # storageClassName: "" # 環境に合わせて設定
